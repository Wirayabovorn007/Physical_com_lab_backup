#include <stdio.h>
#include <ctype.h>

char *correctNameFormat(char *str)
{
    int isHead = 1;
    int i = 0;
    while (str[i]){
        if (isHead)
    }
}

int main(){
    int n;
    scanf("%d", &n);
    char std[n][61];
    for (int i=0; i<n; i++) {
        scanf("%s", )
        std[n] = 
    }
}