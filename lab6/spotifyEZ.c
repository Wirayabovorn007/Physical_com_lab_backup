#include <stdio.h>

int main(){
    long minute;
    int second;
    int n;

    scanf("%ld.%d", &minute, &second);
    scanf("%d", n);

    char songName[n][100];
    for (int i=0; i<n; i++) scanf(" Queue#%d <|> %[^<] <|> %d.%d", );
}